diff --git a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/AndroidForegroundService.java b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/AndroidForegroundService.java
index 3df5ad5..a45fcdc 100644
--- a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/AndroidForegroundService.java
+++ b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/AndroidForegroundService.java
@@ -1,6 +1,7 @@
 package io.capawesome.capacitorjs.plugins.foregroundservice;
 
 import android.app.Notification;
+import android.app.NotificationChannel;
 import android.app.NotificationManager;
 import android.app.PendingIntent;
 import android.app.Service;
@@ -9,12 +10,15 @@ import android.content.Intent;
 import android.os.Build;
 import android.os.Bundle;
 import android.os.IBinder;
+import android.util.Log;
 import androidx.annotation.Nullable;
 import com.getcapacitor.Logger;
 import java.util.ArrayList;
 
 public class AndroidForegroundService extends Service {
 
+    private static final String TAG = "PomoTaskFS";
+
     @Nullable
     @Override
     public IBinder onBind(Intent intent) {
@@ -23,20 +27,44 @@ public class AndroidForegroundService extends Service {
 
     @Override
     public int onStartCommand(Intent intent, int flags, int startId) {
+        Log.d(TAG, "onStartCommand called, intent=" + (intent != null ? "not null" : "NULL"));
+        if (intent == null) {
+            stopSelf();
+            return START_NOT_STICKY;
+        }
         try {
-            String action = intent.getAction();
             Bundle extras = intent.getExtras();
             String channelId = extras.getString("channelId", ForegroundService.DEFAULT_NOTIFICATION_CHANNEL_ID);
             Bundle notificationBundle = extras.getBundle("notification");
-            String body = notificationBundle.getString("body");
             int id = notificationBundle.getInt("id");
             int icon = notificationBundle.getInt("icon");
-            String title = notificationBundle.getString("title");
-            boolean silent = notificationBundle.getBoolean("silent", false);
-            ArrayList<Bundle> buttonsBundle = notificationBundle.getParcelableArrayList("buttons");
             int serviceType = notificationBundle.getInt("serviceType", 0);
+            String title = notificationBundle.getString("title");
+            String body = notificationBundle.getString("body");
+            String action = intent.getAction();
+            boolean isUpdate = ForegroundService.ACTION_UPDATE.equals(action);
+
+            // Ensure channel exists on Android O+
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
+                NotificationManager nm = (NotificationManager) getSystemService(Context.NOTIFICATION_SERVICE);
+                if (nm.getNotificationChannel(channelId) == null) {
+                    Log.w(TAG, "Channel '" + channelId + "' not found, creating default");
+                    NotificationChannel fallbackChannel = new NotificationChannel(
+                        channelId,
+                        "Timer",
+                        NotificationManager.IMPORTANCE_DEFAULT
+                    );
+                    fallbackChannel.setDescription("Timer notification");
+                    nm.createNotificationChannel(fallbackChannel);
+                }
+            }
+
+            // Fallback icon if resource not found
+            if (icon == 0) {
+                Log.w(TAG, "Icon resource not found, using system fallback");
+                icon = android.R.drawable.ic_menu_info_details;
+            }
 
-            PendingIntent contentIntent = buildContentIntent(id);
             Notification.Builder builder;
             if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                 builder = new Notification.Builder(getApplicationContext(), channelId);
@@ -44,33 +72,46 @@ public class AndroidForegroundService extends Service {
                 builder = new Notification.Builder(getApplicationContext());
             }
             builder
-                .setContentTitle(title)
-                .setContentText(body)
-                .setContentIntent(contentIntent)
+                .setContentTitle(title != null ? title : "PomoTasker")
+                .setContentText(body != null ? body : "")
                 .setOngoing(true)
-                .setSmallIcon(icon)
-                .setPriority(Notification.PRIORITY_HIGH)
-                .setOnlyAlertOnce(silent);
-            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.N) {
+                .setAutoCancel(false)
+                .setSmallIcon(icon);
+
+            PendingIntent contentIntent = buildContentIntent(id);
+            if (contentIntent != null) {
+                builder.setContentIntent(contentIntent);
+            }
+
+            boolean silent = notificationBundle.getBoolean("silent", false);
+            // For updates, always use silent to prevent flicker/vibration
+            if (isUpdate) {
+                silent = true;
+            }
+            builder.setPriority(Notification.PRIORITY_HIGH).setOnlyAlertOnce(silent);
+
+            ArrayList<Bundle> buttonsBundle = notificationBundle.getParcelableArrayList("buttons");
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.N && buttonsBundle != null && !buttonsBundle.isEmpty()) {
+                Log.d(TAG, "Adding " + buttonsBundle.size() + " buttons");
                 Notification.Action[] actions = convertBundlesToNotificationActions(
                     buttonsBundle.toArray(new Bundle[buttonsBundle.size()])
                 );
                 builder.setActions(actions);
+            } else {
+                Log.w(TAG, "No buttons in bundle");
             }
 
             Notification notification = builder.build();
-
-            if (ForegroundService.ACTION_UPDATE.equals(action)) {
-                NotificationManager notificationManager = (NotificationManager) getSystemService(Context.NOTIFICATION_SERVICE);
-                notificationManager.notify(id, notification);
+            // Always use startForeground() — updates the notification and maintains foreground state
+            Log.d(TAG, "Calling startForeground");
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q && serviceType != 0) {
+                startForeground(id, notification, serviceType);
             } else {
-                if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q && serviceType != 0) {
-                    startForeground(id, notification, serviceType);
-                } else {
-                    startForeground(id, notification);
-                }
+                startForeground(id, notification);
             }
+            Log.d(TAG, "startForeground succeeded");
         } catch (Exception exception) {
+            Log.e(TAG, "Exception in onStartCommand: " + exception.getMessage(), exception);
             Logger.error(ForegroundServicePlugin.TAG, exception.getMessage(), exception);
         }
         return START_STICKY;
diff --git a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundService.java b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundService.java
index 4a05220..1fce0f0 100644
--- a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundService.java
+++ b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundService.java
@@ -84,7 +84,9 @@ public class ForegroundService {
         notificationBundle.putString("title", title);
         notificationBundle.putBoolean("silent", silent);
         notificationBundle.putParcelableArrayList("buttons", new ArrayList<Bundle>(buttons));
-        notificationBundle.putInt("serviceType", serviceType == null ? 0 : serviceType);
+        // Always use 0 to let Android infer the type from manifest declaration.
+        // This avoids crashes when stale JS passes incorrect serviceType values.
+        notificationBundle.putInt("serviceType", 0);
 
         Context context = plugin.getContext();
         Intent intent = new Intent(context, AndroidForegroundService.class);
@@ -95,11 +97,9 @@ public class ForegroundService {
             intent.setAction(ACTION_UPDATE);
         }
 
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
-            context.startForegroundService(intent);
-        } else {
-            context.startService(intent);
-        }
+        // Always use startService() — never startForegroundService().
+        // startForeground() inside onStartCommand() handles foreground promotion.
+        context.startService(intent);
     }
 
     public void stopForegroundService() {
