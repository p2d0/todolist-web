diff --git a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/AndroidForegroundService.java b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/AndroidForegroundService.java
index 3df5ad5..cf08531 100644
--- a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/AndroidForegroundService.java
+++ b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/AndroidForegroundService.java
@@ -1,6 +1,7 @@
 package io.capawesome.capacitorjs.plugins.foregroundservice;
 
 import android.app.Notification;
+import android.app.NotificationChannel;
 import android.app.NotificationManager;
 import android.app.PendingIntent;
 import android.app.Service;
@@ -9,12 +10,17 @@ import android.content.Intent;
 import android.os.Build;
 import android.os.Bundle;
 import android.os.IBinder;
+import android.util.Log;
 import androidx.annotation.Nullable;
+import androidx.core.app.NotificationCompat;
 import com.getcapacitor.Logger;
 import java.util.ArrayList;
 
 public class AndroidForegroundService extends Service {
 
+    private static final String TAG = "PomoTaskFS";
+    private static ArrayList<Bundle> cachedButtons = null;
+
     @Nullable
     @Override
     public IBinder onBind(Intent intent) {
@@ -23,54 +29,113 @@ public class AndroidForegroundService extends Service {
 
     @Override
     public int onStartCommand(Intent intent, int flags, int startId) {
+        Log.d(TAG, "onStartCommand called, intent=" + (intent != null ? "not null" : "NULL"));
+        if (intent == null) {
+            stopSelf();
+            return START_NOT_STICKY;
+        }
         try {
-            String action = intent.getAction();
             Bundle extras = intent.getExtras();
             String channelId = extras.getString("channelId", ForegroundService.DEFAULT_NOTIFICATION_CHANNEL_ID);
             Bundle notificationBundle = extras.getBundle("notification");
-            String body = notificationBundle.getString("body");
             int id = notificationBundle.getInt("id");
             int icon = notificationBundle.getInt("icon");
+            int serviceType = notificationBundle.getInt("serviceType", 0);
             String title = notificationBundle.getString("title");
+            String body = notificationBundle.getString("body");
             boolean silent = notificationBundle.getBoolean("silent", false);
-            ArrayList<Bundle> buttonsBundle = notificationBundle.getParcelableArrayList("buttons");
-            int serviceType = notificationBundle.getInt("serviceType", 0);
+            String action = intent.getAction();
+            boolean isUpdate = ForegroundService.ACTION_UPDATE.equals(action);
             
-            PendingIntent contentIntent = buildContentIntent(id);
-            Notification.Builder builder;
+            Log.d(TAG, "Building notification: id=" + id + " title=" + title + " body=" + body + " silent=" + silent + " isUpdate=" + isUpdate);
+
+            // Ensure channel exists on Android O+
             if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
-                builder = new Notification.Builder(getApplicationContext(), channelId);
-            } else {
-                builder = new Notification.Builder(getApplicationContext());
+                NotificationManager nm = (NotificationManager) getSystemService(Context.NOTIFICATION_SERVICE);
+                if (nm.getNotificationChannel(channelId) == null) {
+                    Log.w(TAG, "Channel '" + channelId + "' not found, creating default");
+                    NotificationChannel fallbackChannel = new NotificationChannel(
+                        channelId,
+                        "Timer",
+                        NotificationManager.IMPORTANCE_LOW
+                    );
+                    fallbackChannel.setDescription("Timer notification");
+                    nm.createNotificationChannel(fallbackChannel);
+                }
             }
+
+            // Fallback icon if resource not found
+            if (icon == 0) {
+                Log.w(TAG, "Icon resource not found, using system fallback");
+                icon = android.R.drawable.ic_menu_info_details;
+            }
+
+            NotificationCompat.Builder builder = new NotificationCompat.Builder(getApplicationContext(), channelId);
             builder
-                .setContentTitle(title)
-                .setContentText(body)
-                .setContentIntent(contentIntent)
+                .setContentTitle(title != null ? title : "PomoTasker")
+                .setContentText(body != null ? body : "")
                 .setOngoing(true)
+                .setAutoCancel(false)
                 .setSmallIcon(icon)
-                .setPriority(Notification.PRIORITY_HIGH)
-                .setOnlyAlertOnce(silent);
-            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.N) {
-                Notification.Action[] actions = convertBundlesToNotificationActions(
-                    buttonsBundle.toArray(new Bundle[buttonsBundle.size()])
-                );
-                builder.setActions(actions);
+                .setVisibility(NotificationCompat.VISIBILITY_PUBLIC)
+                .setPriority(NotificationCompat.PRIORITY_LOW)
+                .setOnlyAlertOnce(true);
+
+            PendingIntent contentIntent = buildContentIntent(id);
+            if (contentIntent != null) {
+                builder.setContentIntent(contentIntent);
             }
 
-            Notification notification = builder.build();
+            ArrayList<Bundle> buttonsBundle = notificationBundle.getParcelableArrayList("buttons");
+            Log.d(TAG, "buttonsBundle=" + (buttonsBundle != null ? buttonsBundle.size() : "null"));
+            
+            // Cache buttons when received, reuse on updates if JS sends empty array
+            if (buttonsBundle != null && !buttonsBundle.isEmpty()) {
+                cachedButtons = buttonsBundle;
+            }
+            
+            ArrayList<Bundle> buttonsToUse = (buttonsBundle != null && !buttonsBundle.isEmpty()) ? buttonsBundle : cachedButtons;
+            
+            if (buttonsToUse != null && !buttonsToUse.isEmpty()) {
+                Log.d(TAG, "Adding " + buttonsToUse.size() + " buttons");
+                for (int i = 0; i < buttonsToUse.size(); i++) {
+                    Bundle btn = buttonsToUse.get(i);
+                    String btnTitle = btn.getString("title");
+                    int btnId = btn.getInt("id");
+                    Log.d(TAG, "Button " + i + ": title=" + btnTitle + " id=" + btnId);
+                    
+                    Intent btnIntent = new Intent(this, NotificationActionBroadcastReceiver.class);
+                    btnIntent.putExtra("buttonId", btnId);
+                    PendingIntent btnPendingIntent = PendingIntent.getBroadcast(
+                        this,
+                        btnId,
+                        btnIntent,
+                        PendingIntent.FLAG_IMMUTABLE | PendingIntent.FLAG_UPDATE_CURRENT
+                    );
                     
-            if (ForegroundService.ACTION_UPDATE.equals(action)) {
+                    builder.addAction(android.R.drawable.ic_menu_close_clear_cancel, btnTitle, btnPendingIntent);
+                }
+            } else {
+                Log.w(TAG, "No buttons in bundle");
+            }
+
+            Notification notification = builder.build();
+            if (isUpdate) {
+                Log.d(TAG, "Calling NotificationManager.notify for update");
                 NotificationManager notificationManager = (NotificationManager) getSystemService(Context.NOTIFICATION_SERVICE);
                 notificationManager.notify(id, notification);
+                Log.d(TAG, "NotificationManager.notify succeeded");
             } else {
+                Log.d(TAG, "Calling startForeground");
                 if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q && serviceType != 0) {
                     startForeground(id, notification, serviceType);
                 } else {
                     startForeground(id, notification);
                 }
+                Log.d(TAG, "startForeground succeeded");
             }
         } catch (Exception exception) {
+            Log.e(TAG, "Exception in onStartCommand: " + exception.getMessage(), exception);
             Logger.error(ForegroundServicePlugin.TAG, exception.getMessage(), exception);
         }
         return START_STICKY;
@@ -91,25 +156,4 @@ public class AndroidForegroundService extends Service {
         }
         return PendingIntent.getActivity(getApplicationContext(), id, launchIntent, pendingIntentFlags);
     }
-
-    private Notification.Action[] convertBundlesToNotificationActions(Bundle[] bundles) {
-        Notification.Action[] actions = new Notification.Action[bundles.length];
-        for (int i = 0; i < bundles.length; i++) {
-            String title = bundles[i].getString("title");
-            int id = bundles[i].getInt("id");
-
-            Intent intent = new Intent(this, NotificationActionBroadcastReceiver.class);
-            intent.putExtra("buttonId", id);
-            PendingIntent pendingIntent = PendingIntent.getBroadcast(
-                this,
-                id,
-                intent,
-                PendingIntent.FLAG_IMMUTABLE | PendingIntent.FLAG_CANCEL_CURRENT
-            );
-
-            Notification.Action action = new Notification.Action(0, title, pendingIntent);
-            actions[i] = action;
-        }
-        return actions;
-    }
 }
diff --git a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundService.java b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundService.java
index 4a05220..1fce0f0 100644
--- a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundService.java
+++ b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundService.java
@@ -84,7 +84,9 @@ public class ForegroundService {
         notificationBundle.putString("title", title);
         notificationBundle.putBoolean("silent", silent);
         notificationBundle.putParcelableArrayList("buttons", new ArrayList<Bundle>(buttons));
-        notificationBundle.putInt("serviceType", serviceType == null ? 0 : serviceType);
+        // Always use 0 to let Android infer the type from manifest declaration.
+        // This avoids crashes when stale JS passes incorrect serviceType values.
+        notificationBundle.putInt("serviceType", 0);
 
         Context context = plugin.getContext();
         Intent intent = new Intent(context, AndroidForegroundService.class);
@@ -95,11 +97,9 @@ public class ForegroundService {
             intent.setAction(ACTION_UPDATE);
         }
 
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
-            context.startForegroundService(intent);
-        } else {
-            context.startService(intent);
-        }
+        // Always use startService() — never startForegroundService().
+        // startForeground() inside onStartCommand() handles foreground promotion.
+        context.startService(intent);
     }
 
     public void stopForegroundService() {
diff --git a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundServicePlugin.java b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundServicePlugin.java
index aa2c75f..4527c68 100644
--- a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundServicePlugin.java
+++ b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/ForegroundServicePlugin.java
@@ -61,6 +61,19 @@ public class ForegroundServicePlugin extends Plugin {
         handleNotificationTapIntent(intent);
     }
 
+    @Override
+    protected void handleOnResume() {
+        super.handleOnResume();
+        android.content.SharedPreferences prefs = getContext().getSharedPreferences("PomoTaskFS", android.content.Context.MODE_PRIVATE);
+        int pendingButtonId = prefs.getInt("pendingButtonId", -1);
+        if (pendingButtonId != -1) {
+            prefs.edit().remove("pendingButtonId").apply();
+            JSObject result = new JSObject();
+            result.put("buttonId", pendingButtonId);
+            notifyListeners(BUTTON_CLICKED_EVENT, result);
+        }
+    }
+
     // Notification taps are delivered via an intent extra rather than a static method call so that
     // the tap is always handled by the current plugin instance. A static bridge reference can go
     // stale when the activity is recreated (e.g. after a configuration change or process death),
@@ -89,6 +102,23 @@ public class ForegroundServicePlugin extends Plugin {
         }
     }
 
+    @PluginMethod
+    public void checkPendingButtonClick(PluginCall call) {
+        android.content.SharedPreferences prefs = getContext().getSharedPreferences("PomoTaskFS", android.content.Context.MODE_PRIVATE);
+        int pendingButtonId = prefs.getInt("pendingButtonId", -1);
+        android.util.Log.d("PomoTaskFS", "checkPendingButtonClick: pendingButtonId=" + pendingButtonId);
+        JSObject result = new JSObject();
+        if (pendingButtonId != -1) {
+            prefs.edit().remove("pendingButtonId").apply();
+            result.put("buttonId", pendingButtonId);
+            android.util.Log.d("PomoTaskFS", "checkPendingButtonClick: returning buttonId=" + pendingButtonId);
+            call.resolve(result);
+        } else {
+            result.put("buttonId", null);
+            call.resolve(result);
+        }
+    }
+
     @PluginMethod
     public void startForegroundService(PluginCall call) {
         try {
@@ -114,6 +144,7 @@ public class ForegroundServicePlugin extends Plugin {
             boolean silent = call.getBoolean("silent", false);
             Integer serviceType = call.getInt("serviceType");
             JSArray buttons = call.getArray("buttons", new JSArray());
+            android.util.Log.d("PomoTaskPlugin", "buttons JSArray: " + buttons.toString() + " length=" + buttons.length());
 
             ArrayList<Bundle> buttonBundles = new ArrayList<>();
             for (int i = 0; i < buttons.length(); i++) {
@@ -256,11 +287,14 @@ public class ForegroundServicePlugin extends Plugin {
     }
 
     public static void onButtonClicked(int buttonId) {
+        android.util.Log.d("PomoTaskFS", "onButtonClicked called, buttonId=" + buttonId);
         try {
             ForegroundServicePlugin plugin = ForegroundServicePlugin.getForegroundServicePluginInstance();
             if (plugin == null) {
+                android.util.Log.w("PomoTaskFS", "onButtonClicked: plugin instance is null");
                 return;
             }
+            android.util.Log.d("PomoTaskFS", "onButtonClicked: notifying listeners");
             plugin.handleButtonClicked(buttonId);
         } catch (Exception exception) {
             Logger.error(ForegroundServicePlugin.TAG, exception.getMessage(), exception);
@@ -297,7 +331,18 @@ public class ForegroundServicePlugin extends Plugin {
     private void handleButtonClicked(int buttonId) {
         JSObject result = new JSObject();
         result.put("buttonId", buttonId);
-        notifyListeners(BUTTON_CLICKED_EVENT, result);
+        notifyListeners(BUTTON_CLICKED_EVENT, result, true);
+        // Save for replay when app resumes (WebView may be paused)
+        android.content.SharedPreferences prefs = getContext().getSharedPreferences("PomoTaskFS", android.content.Context.MODE_PRIVATE);
+        prefs.edit().putInt("pendingButtonId", buttonId).apply();
+        // Stop the foreground service immediately so the notification disappears
+        if (buttonId == 1 && implementation != null) {
+            try {
+                implementation.stopForegroundService();
+            } catch (Exception e) {
+                Logger.error(TAG, e.getMessage(), e);
+            }
+        }
     }
 
     private static ForegroundServicePlugin getForegroundServicePluginInstance() {
diff --git a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/NotificationActionBroadcastReceiver.java b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/NotificationActionBroadcastReceiver.java
index 3afa80f..4932d77 100644
--- a/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/NotificationActionBroadcastReceiver.java
+++ b/node_modules/@capawesome-team/capacitor-android-foreground-service/android/src/main/java/io/capawesome/capacitorjs/plugins/foregroundservice/NotificationActionBroadcastReceiver.java
@@ -9,8 +9,10 @@ public class NotificationActionBroadcastReceiver extends BroadcastReceiver {
 
     @Override
     public void onReceive(Context context, Intent intent) {
+        android.util.Log.d("PomoTaskFS", "BroadcastReceiver onReceive called");
         try {
             int buttonId = intent.getExtras().getInt("buttonId");
+            android.util.Log.d("PomoTaskFS", "BroadcastReceiver buttonId=" + buttonId);
             ForegroundServicePlugin.onButtonClicked(buttonId);
         } catch (Exception exception) {
             Logger.error(ForegroundServicePlugin.TAG, exception.getMessage(), exception);
diff --git a/node_modules/@capawesome-team/capacitor-android-foreground-service/dist/esm/definitions.d.ts b/node_modules/@capawesome-team/capacitor-android-foreground-service/dist/esm/definitions.d.ts
index c636312..3fc5e85 100644
--- a/node_modules/@capawesome-team/capacitor-android-foreground-service/dist/esm/definitions.d.ts
+++ b/node_modules/@capawesome-team/capacitor-android-foreground-service/dist/esm/definitions.d.ts
@@ -91,6 +91,14 @@ export interface ForegroundServicePlugin {
      * @since 6.1.0
      */
     deleteNotificationChannel(options: DeleteNotificationChannelOptions): Promise<void>;
+    /**
+     * Check for a pending button click that occurred while the app was in the background.
+     *
+     * Only available on Android.
+     *
+     * @since 8.1.0
+     */
+    checkPendingButtonClick(): Promise<ButtonClickedEvent | null>;
     /**
      * Called when a notification button is clicked.
      *
